"""Write Python code to answer the questions about each image."""
from utils import open_image, generate_captions, answer_from_captions
"""
API Reference:
open_image(path: str) -> Image
generate_captions(img: Image, subject: List[str]) -> List[str]
answer_from_captions(img: Image, captions: List[str]) -> str
"""
